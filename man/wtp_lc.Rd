% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wtp_lc.R
\name{wtp_lc}
\alias{wtp_lc}
\title{Compute willingness-to-pay for latent classes in an LC model}
\usage{
wtp_lc(modelname, pricestub, excludestub = "delta")
}
\arguments{
\item{modelname}{An object returned by `apollo_estimate()` (or similar), containing the LC model results (including `LL0` and `estimate`).}

\item{pricestub}{A character string specifying the cost or price parameter name prefix (e.g., "cost_", "b_price_") to use in the WTP calculation. The class letter will be appended internally.}

\item{excludestub}{A character string specifying a parameter name prefix to exclude when subsetting coefficients (e.g., "delta_"). Defaults to "delta_." This is used in the internal grep pattern to avoid mixing in parameters from the membership function including constants.}
}
\value{
A named list. Each element (e.g. `Class 1`, `Class 2`, ...) is a `data.frame` containing:
\itemize{
  \item WTP estimates for the specified `pricestub` parameters across classes.
  \item Associated statistics (p-values, confidence intervals) extracted from the model output.
}
}
\description{
This function wraps the [wtp()] function to compute WTP estimates across all classes of a latent class (LC) model estimated with the Apollo package.
}
\examples{
\dontrun{
  # Estimate your LC model first
  mod <- apollo_estimate(...)
  # Compute WTP using default cost stub, excluding "delta_" suffix
  results <- wtp_lc(mod, "cost_")
  # Compute WTP using a price stub and custom exclusion
  results2 <- wtp_lc(mod, "b_price_", excludestub = "delta_")
}

}
\seealso{
[wtp()], [apollo::apollo_modelOutput()]
}
