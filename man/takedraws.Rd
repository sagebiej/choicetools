% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/takedraws.R
\name{takedraws}
\alias{takedraws}
\title{Draw Samples from a Multivariate Normal Distribution}
\usage{
takedraws(n = 10000, beta, vc)
}
\arguments{
\item{n}{Integer. The number of samples to draw (default: `10000`).}

\item{beta}{Numeric vector. The mean (point estimate) of the distribution.}

\item{vc}{Numeric matrix. The variance-covariance matrix of the distribution.}
}
\value{
A numeric matrix of dimension `n x length(beta)`, where each row is a draw
        from the multivariate normal distribution.
}
\description{
This function generates random draws from a multivariate normal distribution
given a point estimate (`beta`) and a variance-covariance matrix (`vc`).
It uses the Cholesky decomposition for efficient sampling.
}
\examples{
beta <- c(1.5, 2.0)
vc <- matrix(c(0.1, 0.02, 0.02, 0.2), nrow = 2)
draws <- takedraws(n = 1000, beta, vc)
head(draws)

}
